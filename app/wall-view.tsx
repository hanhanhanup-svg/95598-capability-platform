'use client';
import { useState } from 'react';
import { Users, Target, BriefcaseBusiness, ArrowUpRight, Download, ArrowRight, ShieldCheck, Layers3 } from 'lucide-react';
import { roles, people, capabilities, peopleFor, roleById, capById, personMetrics, roleMetrics, targetsFor, exportCsv, type Person } from './model';
import { Picker, Segments, SearchBox, Badge, DataTable, PanelTitle, Note, Meter, Stars } from './ui-parts';
import { RoleMatrix, type DetailRef } from './graph-view';

export function WallView({roleId,setRoleId,onDetail,onGrowth}:{roleId:string;setRoleId:(id:string)=>void;onDetail:(d:DetailRef)=>void;onGrowth:(p:Person)=>void}){
 const [tab,setTab]=useState('people');const [query,setQuery]=useState('');const [status,setStatus]=useState('all');const r=roleById(roleId);const m=roleMetrics(roleId);
 const shown=peopleFor(roleId).filter(p=>{const match=(`${p.name}${p.id}${p.team}`).toLowerCase().includes(query.trim().toLowerCase());const v=Object.values(p.capabilities);return match&&(status==='all'||status==='gap'&&personMetrics(p).achieved<personMetrics(p).known||status==='evidence'&&v.some(v=>v.evidenceStatus==='missing'||v.evidenceStatus==='expired')||status==='review'&&v.some(v=>v.evidenceStatus==='review'))});
 return <><div className="role-selector-grid">{roles.map(x=><button key={x.id} className={`role-tile ${x.id===roleId?'active':''}`} onClick={()=>setRoleId(x.id)}><BriefcaseBusiness size={19}/><b>{x.name}</b><span>{peopleFor(x.id).length} 人<ArrowUpRight size={14}/></span></button>)}</div><section className="panel"><PanelTitle title="一墙四层，岗位与人员使用同一把标尺" sub="岗位能力标准 / 人员能力映射 / 差距与成长 / 队伍结构" icon={<Layers3 size={18}/> }><Segments value={tab} onChange={setTab} options={[{value:'people',label:'人员能力墙'},{value:'standard',label:'岗位标尺'},{value:'team',label:'队伍结构'}]}/></PanelTitle>{tab==='standard'?<RoleMatrix onDetail={onDetail}/>:tab==='team'?<TeamView onDetail={onDetail}/>:<><div className="wall-banner"><div><span className="eyebrow">POSITION PROFILE</span><h2>{r.name}</h2><div className="tag-list"><Badge>{r.capabilityIds.length} 项核心能力</Badge><Badge tone="blue">{r.scenarioIds.length} 个业务场景</Badge><Badge tone="gray">DEMO-BO-1.0</Badge></div></div><div className="wall-banner-metrics"><div><b>{m.count}<small>人</small></b><span>在岗人员</span></div><div><b>{m.score}<small>%</small></b><span>对照三星标准</span></div><div><b>{m.coverage}<small>%</small></b><span>证据覆盖率</span></div></div><button className="button outline" onClick={()=>onDetail({type:'role',id:roleId})}>查看岗位标准<ArrowRight size={15}/></button></div><div className="table-toolbar"><div className="inline-controls"><SearchBox value={query} onChange={setQuery} placeholder="搜索人员姓名、班组或编码"/><Picker label="能力记录状态" value={status} onChange={setStatus} options={[{value:'all',label:'全部状态'},{value:'gap',label:'有能力差距'},{value:'evidence',label:'需补充证据'},{value:'review',label:'有待复核记录'}]}/></div><button className="button outline" onClick={()=>exportCsv('岗位能力墙_模拟数据.csv',['人员','岗位','班组','模拟当前星级','目标星级','参考匹配度','证据覆盖率'],shown.map(p=>{const m=personMetrics(p);return [p.name,r.name,p.team,p.formalStars,p.targetStars,m.score??'无法判断',m.coverage]}))}><Download size={15}/>导出当前视图</button></div><DataTable headers={['人员 / 班组','当前星级（模拟）','成长目标','参考匹配度','依据覆盖','主要成长方向','操作']} rows={shown.map(p=>{const m=personMetrics(p);return [<button className="person-cell" onClick={()=>onDetail({type:'person',id:p.id})}><span className="avatar">{p.name.slice(-2)}</span><span><b>{p.name}</b><small>{p.team} · {p.id}</small></span></button>,<div className="star-cell"><Stars value={p.formalStars}/><small>模拟已生效</small></div>,<span className="goal-value">{p.formalStars} 星 <ArrowRight size={12}/> {p.targetStars} 星</span>,m.score===null?<Badge tone="gray">依据不足</Badge>:<Meter value={m.score} label={`${p.name}参考匹配度`}/>,<div><b>{m.known} / {m.total} 项</b>{m.unknown>0&&<small className="cell-note">{m.unknown} 项需补证或复核</small>}</div>,<div className="growth-cell"><b>{capById(p.growthFocus.capabilityId).name}</b><small>{p.growthFocus.nextAction}</small></div>,<button className="text-button" onClick={()=>onGrowth(p)}>成长路径<ArrowUpRight size={14}/></button>]})}/><div className="table-summary">共 {shown.length} 人 · 当前岗位 {peopleFor(roleId).length} 人 · 支持点击姓名查看能力、差距与证据</div><Note>参考匹配度 = 可采信能力的“当前等级 / 目标等级”（单项最高100%）均值；缺失、过期、待复核记录不计零分。请结合依据覆盖率判断，百分比不替代正式评价。表中目标采用每个人的成长目标星级。</Note></>}</section></>
}

function TeamView({onDetail}:{onDetail:(d:DetailRef)=>void}){return <div className="team-view"><div className="team-top"><div className="team-summary"><Users size={27}/><span>模拟队伍总人数</span><b>{people.length}</b><p>{roles.length} 类岗位 · {new Set(people.map(p=>p.team)).size} 个班组</p></div><div className="star-distribution"><h3>当前星级分布 <small>模拟已生效结果</small></h3>{[1,2,3,4,5].map(v=>{const count=people.filter(p=>p.formalStars===v).length;return <div className="distribution-row" key={v}><span>{v} 星</span><Meter value={count/people.length*100} label={`${v}星人员占比`}/><b>{count} 人</b></div>})}</div></div><DataTable headers={['岗位','人员规模','参考匹配度 / 三星标准','证据覆盖率','存在能力差距','查看']} rows={roles.map(r=>{const m=roleMetrics(r.id);return [r.name,`${m.count} 人`,<Meter value={m.score}/>,`${m.coverage}%`,`${m.gaps} 人`,<button className="text-button" onClick={()=>onDetail({type:'role',id:r.id})}>岗位画像<ArrowUpRight size={14}/></button>]})}/><Note>人员数、星级分布与岗位统计均从同一组 40 名合成人员汇总；管理视图用于观察队伍结构，不替代人员考核决定。</Note></div>}
